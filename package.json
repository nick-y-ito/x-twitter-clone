{
	"name": "z",
	"version": "1.0.0",
	"description": "Z is a clone app of X (Twitter)",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"frontend": "npm run dev --prefix frontend",
		"server": "npm start --prefix server",
		"dev": "concurrently \"npm run frontend\" \"npm run server\"",
		"build": "npm run build --prefix frontend",
		"start": "npm run server --prefix server"
	},
	"workspaces": [
		"frontend",
		"server"
	],
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"concurrently": "^8.2.2"
	}
}
